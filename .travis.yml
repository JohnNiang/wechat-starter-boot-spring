language: java
jdk:
  - oraclejdk8
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
jobs:
  include:
    - stage: build
      script: gradle build -x test
    - stage: test
      script: gradle test
stages:
  - build
  - test
branches:
  only:
    - master
